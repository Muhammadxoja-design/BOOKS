{
  "name": "books-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node scripts/run-root.mjs",
    "start": "node scripts/run-root.mjs",
    "install:all": "npm install --legacy-peer-deps && npm install --workspace=frontend --legacy-peer-deps && npm install --workspace=backend --legacy-peer-deps",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "seed": "npm run seed --workspace=backend",
    "prisma:push": "npm run prisma:push --workspace=backend",
    "prisma:generate": "npm run prisma:generate --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
